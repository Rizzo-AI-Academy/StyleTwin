VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Backend base URL.
# - Leave empty in local dev: the Vite proxy forwards /api -> http://localhost:8000.
# - Set to the deployed backend origin in production (e.g. https://api.styletwin.app).
VITE_API_BASE_URL=
